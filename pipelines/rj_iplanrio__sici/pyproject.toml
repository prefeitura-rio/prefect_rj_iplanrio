[project]
name = "rj_iplanrio__sici"
version = "0.1.0"
description = "Pipeline para dump de dados da API SOAP do SICI para BigQuery"
dependencies = [
    "prefect_rj_iplanrio",
    "zeep",
    "pandas",
    "pytz",
]

[tool.uv.sources]
prefect_rj_iplanrio = { workspace = true }
