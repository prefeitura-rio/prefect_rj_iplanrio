name: rj_smfp__iptu_inadimplentes
prefect-version: 3.4.3

build:
  - prefect.deployments.steps.run_shell_script:
      id: get-commit-hash
      script: git rev-parse --short HEAD
      stream_output: false
  - prefect_docker.deployments.steps.build_docker_image:
      id: build-image
      requires: prefect-docker>=0.6.5
      image_name: ghcr.io/prefeitura-rio/prefect_rj_iplanrio/deployments
      tag: "rj_smfp__iptu_inadimplentes-{{ get-commit-hash.stdout }}"
      dockerfile: pipelines/rj_smfp__iptu_inadimplentes/Dockerfile

push:
  - prefect_docker.deployments.steps.push_docker_image:
      requires: prefect-docker>=0.6.5
      image_name: "{{ build-image.image_name }}"
      tag: "{{ build-image.tag }}"

pull:
  - prefect.deployments.steps.set_working_directory:
      directory: /opt/prefect/prefect_rj_iplanrio

deployments:
  # Deployment de staging para testes
  - name: rj-smfp--iptu-inadimplentes--staging
    version: "{{ build-image.tag }}"
    entrypoint: pipelines/rj_smfp__iptu_inadimplentes/flow.py:rj_smfp__iptu_inadimplentes
    work_pool:
      name: k3s-pool
      work_queue_name: default
      job_variables:
        image: "{{ build-image.image_name }}:{{ build-image.tag }}"
        command: uv run --package rj_smfp__iptu_inadimplentes -- prefect flow-run execute
        secretName: prefect-jobs-secrets
        image_pull_policy: Always

  # - name: rj-smfp--iptu-inadimplentes--prod
  #   version: "{{ get-commit-hash.stdout }}"
  #   entrypoint: pipelines/rj_smfp__iptu_inadimplentes/flow.py:rj_smfp__iptu_inadimplentes
  #   work_pool:
  #     name: k3s-pool
  #     work_queue_name: default
  #     job_variables:
  #       image: "{{ build-image.image_name }}:{{ build-image.tag }}"
  #       command: uv run --package rj_smfp__iptu_inadimplentes -- prefect flow-run execute
  #       secretName: prefect-jobs-secrets
  #       image_pull_policy: Always

  #   schedules:
  #   # ========================================
  #   # TABELA: perfil_inadimplente_v2
  #   # ========================================

  #   # Tabela: perfil_inadimplente_v2
  #   # Descrição: Perfil completo de contribuintes inadimplentes do IPTU
  #   # Fonte: DBINAD.IPTU.IPTU
  #   # Frequência: Semanal (a cada 7 dias)
  #   # Modo: Overwrite (substitui dados completos semanalmente)
  #   - interval: 604800  # 7 dias em segundos (7 * 24 * 60 * 60)
  #     anchor_date: "2022-10-30T23:00:00"  # Data original do schedule Prefect 1.4
  #     timezone: America/Sao_Paulo
  #     slug: perfil-inadimplente-v2-weekly
  #     parameters:
  #       table_id: perfil_inadimplente_v2
