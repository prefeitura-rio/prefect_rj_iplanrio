"""
External I/O adapters for the NF pipeline: BigQuery, GCS, local SQLite cache.

NF-pipeline-specific wrappers around the generic clients in
``iplanrio_agent_toolkit`` — no business logic lives here, just reading/writing
external systems.
"""
