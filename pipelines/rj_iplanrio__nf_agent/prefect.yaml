name: rj_iplanrio__nf_agent
prefect-version: 3.4.3

build:
  - prefect.deployments.steps.run_shell_script:
      id: get-commit-hash
      script: git rev-parse --short HEAD
      stream_output: false

  - prefect_docker.deployments.steps.build_docker_image:
      id: build-image
      requires: prefect-docker>=0.6.5
      image_name: ghcr.io/prefeitura-rio/prefect_rj_iplanrio/deployments
      tag: "rj_iplanrio__nf_agent-{{ get-commit-hash.stdout }}"
      dockerfile: pipelines/rj_iplanrio__nf_agent/Dockerfile

push:
  - prefect_docker.deployments.steps.push_docker_image:
      requires: prefect-docker>=0.6.5
      image_name: "{{ build-image.image_name }}"
      tag: "{{ build-image.tag }}"

pull:
  - prefect.deployments.steps.set_working_directory:
      directory: /opt/prefect/prefect_rj_iplanrio

deployments:
  - name: rj-iplanrio--nf-agent--staging
    tags:
      - environment:staging
      - severity:medium
      - code_owner:felipekhouri
    version: "{{ build-image.tag }}"
    entrypoint: pipelines/rj_iplanrio__nf_agent/flow.py:rj_iplanrio__nf_agent
    concurrency_limit: 1
    work_pool:
      name: default-pool
      work_queue_name: default
      job_variables:
        image: "{{ build-image.image_name }}:{{ build-image.tag }}"
        command: uv run --package rj_iplanrio__nf_agent -- prefect flow-run execute
        secretName: prefect-jobs-secrets-staging
        image_pull_policy: Always
        env:
          # A imagem não tem .git; o commit vai por variável para rastrear a versão nas linhas de saída.
          GIT_COMMIT_SHA: "{{ get-commit-hash.stdout }}"
    # Desvio deliberado do STYLEGUIDE §9.3 (staging sem schedules): sem isso,
    # acompanhar uma sessão em andamento exige disparo manual repetido. O
    # parâmetro padrão do flow (acao=acompanhar) garante que o schedule nunca
    # submete PDF novo sozinho — só submeter continua manual. tasks.py pausa
    # este agendamento quando não sobra sessão ativa e reativa ao submeter,
    # então o intervalo aqui só importa enquanto algo está de fato rodando.
    schedules:
      - interval: 3600
        anchor_date: "2025-01-01T00:00:00"
        timezone: America/Sao_Paulo
        slug: acompanhar-sessoes

  - name: rj-iplanrio--nf-agent--prod
    tags:
      - environment:prod
      - severity:medium
      - code_owner:felipekhouri
    version: "{{ get-commit-hash.stdout }}"
    entrypoint: pipelines/rj_iplanrio__nf_agent/flow.py:rj_iplanrio__nf_agent
    concurrency_limit: 1
    work_pool:
      name: default-pool
      work_queue_name: default
      job_variables:
        image: "{{ build-image.image_name }}:{{ build-image.tag }}"
        command: uv run --package rj_iplanrio__nf_agent -- prefect flow-run execute
        secretName: prefect-jobs-secrets
        image_pull_policy: Always
        env:
          GIT_COMMIT_SHA: "{{ get-commit-hash.stdout }}"
    schedules:
      - interval: 3600
        anchor_date: "2025-01-01T00:00:00"
        timezone: America/Sao_Paulo
        slug: acompanhar-sessoes
