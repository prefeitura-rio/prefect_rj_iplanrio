[project]
name = "rj_iplanrio__test"
version = "1.0.0"
description = "Pipeline de referência para validar o logger unificado, a arquitetura em três camadas (flow/tasks/utils) e o ciclo completo de uma pipeline no workspace."
dependencies = [
    "prefect_rj_iplanrio",
]

[tool.uv.sources]
prefect_rj_iplanrio = { workspace = true }
